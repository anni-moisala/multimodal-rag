# Multimodal RAG using ColQwen and Qdrant

This project implements multimodal RAG using **ColQwen**, a document retrieval model based on ColPali using vision LLMs + late interaction to embed and retrieve image representation of document pages.
